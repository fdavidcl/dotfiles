# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# BEGIN i3 configuration

# Custom wallpaper
# set $wallpaper /media/datos/Pictures/lorca.jpg
# set $wallpaper /media/datos/Pictures/Wallpapers/4k/destination_4k.jpg
# set $wallpaper /media/datos/Pictures/Wallpapers/4k/red_panda_unsplash.jpg
# set $wallpaper /media/datos/Pictures/a44jaengranada.jpg
#  set $wallpaper /media/datos/Pictures/Wallpapers/MirrorsEdge/Captura de pantalla de 2016-03-11 01-00-09.png
# set $wallpaper /media/datos/Pictures/canon-eos-400d/2017-granada/IMG_4539.JPG
# set $wallpaper /media/datos/Pictures/Wallpapers/scorpio.jpg
#set $wallpaper /home/fdavidcl/Imágenes/sony-dsc-hx90v/2017/08/nordic-countries/18/DSC00539.JPG


# Terminal
set $TERMINAL xfce4-terminal

set_from_resource $base00 i3wm.color0 #151515
set_from_resource $base01 i3wm.color1 #202020
set_from_resource $base02 i3wm.color2 #303030
set_from_resource $base03 i3wm.color3 #505050
set_from_resource $base04 i3wm.color4 #b0b0b0
set_from_resource $base05 i3wm.color5 #d0d0d0
set_from_resource $base06 i3wm.color6 #e0e0e0
set_from_resource $base07 i3wm.color7 #f5f5f5
set_from_resource $base08 i3wm.color8 #fb9fb1
set_from_resource $base09 i3wm.color9 #eda987
set_from_resource $base0A i3wm.color10 #ddb26f
set_from_resource $base0B i3wm.color11 #acc267
set_from_resource $base0C i3wm.color12 #12cfc0
set_from_resource $base0D i3wm.color13 #6fc2ef
set_from_resource $base0E i3wm.color14 #e1a3ee
set_from_resource $base0F i3wm.color15 #deaf8f

# Colors
set $opacity             00

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Noto Sans 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec xfce4-terminal
# start an editor
bindsym $mod+e exec emacsclient -c /media/datos/Documents
# start a file manager
bindsym $mod+f exec thunar

# kill focused window
bindsym $mod+Shift+q kill
bindsym Mod1+F4 kill

# start dmenu (a program launcher)
# bindsym $mod+d exec --no-startup-id j4-dmenu-desktop --display-binary

# change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
# bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+F10 layout stacking
bindsym $mod+F11 layout tabbed
bindsym $mod+F12 layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
#bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# workspace names
set $w1 "1:  "
set $w2 "2: 2 "
set $w3 "3: 3 "
set $w4 "4: 4 "
set $w5 "5: 5 "
set $w6 "6: 6 "
set $w7 "7: 7 "
set $w8 "8: 8 "
set $w9 "9:  "
set $w10 "10:  "
set $dash "0:  "
set $w11 "11: F1 "
set $w12 "12: F2 "
set $w13 "13: F3 "
set $w14 "14: F4 "
set $w15 "15: F5 "
set $w16 "16: F6 "
set $w17 "17: F7 "
set $w18 "18: F8 "
set $w19 "19: F9 "

set $w1 "number 1"
set $w2 "number 2"
set $w3 "number 3"
set $w4 "number 4"
set $w5 "number 5"
set $w6 "number 6"
set $w7 "number 7"
set $w8 "number 8"
set $w9 "number 9"
set $w10 "number 10"
set $w11 "number 11"
set $w12 "number 12"
set $w13 "number 13"
set $w14 "number 14"
set $w15 "number 15"
set $w16 "number 16"
set $w17 "number 17"
set $w18 "number 18"
set $w19 "number 19"

# switch to workspace
workspace_auto_back_and_forth yes
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

bindsym $mod+F1 workspace number 11
bindsym $mod+F2 workspace number 12
bindsym $mod+F3 workspace number 13
bindsym $mod+F4 workspace number 14
bindsym $mod+F5 workspace number 15
bindsym $mod+F6 workspace number 16
bindsym $mod+F7 workspace number 17
bindsym $mod+F8 workspace number 18
bindsym $mod+F9 workspace number 19
# bindsym $mod+F12 exec ~/.i3/load_dashboard.sh $dash
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace prev

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10
bindsym $mod+Shift+F1 move container to workspace number 11
bindsym $mod+Shift+F2 move container to workspace number 12
bindsym $mod+Shift+F3 move container to workspace number 13
bindsym $mod+Shift+F4 move container to workspace number 14
bindsym $mod+Shift+F5 move container to workspace number 15
bindsym $mod+Shift+F6 move container to workspace number 16
bindsym $mod+Shift+F7 move container to workspace number 17
bindsym $mod+Shift+F8 move container to workspace number 18
bindsym $mod+Shift+F9 move container to workspace number 19

bindsym $mod+End exec --no-startup-id python ~/.config/i3/new_workspace.py

# assign [class="^Firefox$"] $w1
# assign [class="^Clementine$"] $w9
# assign [class="^Tweetdeck"] $w10
# assign [class="^Telegram Desktop$"] $w10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+Escape restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym ntilde resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r       resize grow width 10 px or 10 ppt
bindsym $mod+Shift+r resize shrink width 10 px or 10 ppt
bindsym $mod+t       resize grow height 10 px or 10 ppt
bindsym $mod+Shift+t resize shrink height 10 px or 10 ppt

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#         i3bar_command i3bar
# #       mode hide
#         position top
#         status_command i3status-rs ~/.config/i3/i3status-rs.toml
#         font pango:Noto Sans, FontAwesome 10
#         separator_symbol " "
#         tray_padding 0
#         strip_workspace_numbers yes

#     colors {
#         background $base00
#         separator  $base01
#         statusline $base04

#         # State             Border  BG      Text
#         focused_workspace   $base05 $base0C $base00
#         active_workspace    $base05 $base03 $base00
#         inactive_workspace  $base03 $base01 $base05
#         urgent_workspace    $base08 $base08 $base00
#         binding_mode        $base00 $base0A $base00
#     }
# }

# exec --no-startup-id xfce4-panel --disable-wm-check

# Basic color configuration using the Base16 variables for windows and borders.
# Property Name         Border  BG      Text    Indicator Child Border
client.focused          $base05 $base0D $base00 $base0D $base03
client.focused_inactive $base01 $base01 $base05 $base03 $base01
client.unfocused        $base01 $base00 $base05 $base01 $base01
client.urgent           $base08 $base08 $base00 $base08 $base08
client.placeholder      $base00 $base00 $base05 $base00 $base00
client.background $base07

# rofi launcher
# bindsym Mod1+Tab exec rofi -show window -c ~/.config/rofi/config
bindsym $mod+space exec rofi -show drun -c ~/.config/rofi/config
# bindsym $mod+space exec rofi -show combi -c ~/.config/rofi/config


# exposé-like task switcher
# bindsym Mod1+Tab exec skippy-xd-fix

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer set Master 5%+
 #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id amixer set Master 5%- #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id amixer -D pulse set Master 1+ toggle # mute sound

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec light -p -A 5 # increase screen brightness
bindsym XF86MonBrightnessDown exec light -p -U 5 # decrease screen brightness

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# i3-gaps
for_window [class="^.*"] border pixel 0
gaps inner 16
gaps outer 0
# smart_gaps on

# screenshots
bindsym Print exec scrot /media/datos/Pictures/Screenshots/screenshot_`date +%Y-%m-%d_%H:%M:%S`.png
bindsym $mod+Shift+p exec scrot -s /media/datos/Pictures/Screenshots/screenshot_`date +%Y-%m-%d_%H:%M:%S`.png
bindsym $mod+Print exec scrot -u /media/datos/Pictures/Screenshots/screenshot_`date +%Y-%m-%d_%H:%M:%S`.png

# Power management
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym e exec --no-startup-id i3exit logout, mode "default"
    bindsym s exec --no-startup-id systemctl suspend
    bindsym h exec --no-startup-id i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-idi 3exit shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
# bindsym $mod+Escape mode "$mode_system"

# multimonitor setup
# set $hdmi "11: hdmi "
# workspace $hdmi output HDMI-1
# bindsym $mod+F7 workspace --no-auto-back-and-forth $hdmi
# bindsym $mod+Shift+F7 move container to workspace $hdmi

# set $dp "12: dp "
# workspace $dp output DP-1
# bindsym $mod+F8 workspace --no-auto-back-and-forth $dp
# bindsym $mod+Shift+F8 move container to workspace $dp

# notes workspace
bindsym $mod+n exec "xdotool search --name 'notes.org' windowactivate && i3-msg kill || emacsclient -c /media/datos/Sync/notes.org"
for_window [class="Emacs" title="notes.org"] floating enable
for_window [class="Emacs" title="notes.org"] resize set 1920 1280
for_window [class="Emacs" title="notes.org"] move position center

# Timelog window
bindsym $mod+c exec "xdotool search --name 'timelog.org' windowactivate && i3-msg kill || emacsclient -c /media/datos/Documents/timelog.org"
for_window [class="Emacs" title="timelog.org"] floating enable
for_window [class="Emacs" title="timelog.org"] resize set 1920 1280
for_window [class="Emacs" title="timelog.org"] move position center

# END i3 configuration
# BEGIN startup programs

# fill dashboard
# exec ~/.i3/load_dashboard.sh $dash

# exec sranrdrd autorandr --change --default

exec yabar

# wallpaper
# exec_always wal -t -i "$wallpaper"
exec --no-startup-id nitrogen --restore

# Fix DPI in GTK apps:
exec /usr/lib/gnome-settings-daemon/gsd-xsettings
# Fix DPI broken by GSD in non-GTK apps:
# exec_always xrdb -merge ~/.Xresources
# exec xfsettingsd
# exec xrandr --output eDP1 --mode 3840x2160 --dpi 192 --preferred

# disable system bell (beep)
exec_always xset -b

# set touchpad settings
exec_always xinput set-prop 14 298 0.5 # fast acceleration
exec_always xinput set-prop 14 286 1 # enables tapping
exec_always xinput set-prop 14 294 1 # enables natural scrolling

# compositing manager
exec_always compton --config ~/.config/compton/compton.conf

# exec brightside&

# disable screen dim after 10min
# exec_always xset s off -dpms
exec_always xset s off
exec_always xset s noblank
exec_always xset -dpms
# exec xfce4-power-manager

# exec_always python ~/.config/i3/autoname_workspaces.py

# redshift (same as f.lux)
exec redshift -l37.1809462:-3.5912717
# END startup programs
